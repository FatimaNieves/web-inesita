---
interface Props {
    class?: string;
}

const { class: className = 'h-10 w-auto' } = Astro.props;
---

<img src="/images/inesita/logo.jpg" alt="Inesita · Café de Especialidad" class={`${className} mix-blend-multiply`} />
